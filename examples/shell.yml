name: Shell Example
loglevel: 1
deployment:
  - name: shell
    config:
      host: localhost
      commands:
        - echo "foo command running!"
        - echo "doing something..."
        - echo "done!"
      rollback_commands:
        - echo "Rollback command running!"
        - echo "./panic --string lucky this is a test"
        - echo "rollback done!"